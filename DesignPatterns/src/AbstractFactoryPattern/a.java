package AbstractFactoryPattern;

public class a {

	/*
	 * 1.Create an interface for Shapes.
	 * 2.Create concrete classes like square, rectangle, roundedsquare, roundedsquare implementing 
	 * the same interface.
	 * 3.Create an Abstract class (AbstractFactory) to get factories for Normal and Rounded Shape Objects.
	 * 4.Create Factory like shapeFactory and roundedShapeFactory classes extending AbstractFactory to 
	 * generate object of concrete class based on given information.
	 * 5.Create a Factory generator/producer class to get factories by passing an information such as Shape.
	 * 6.Use the FactoryProducer to get AbstractFactory in order to get factories of concrete classes 
	 * by passing an information such as type.
	 */
	
}
