package ChainPattern;

public class a {

	/*
	 * We have created an abstract class AbstractLogger with a level of logging. Then we have created three 
	 * types of loggers extending the AbstractLogger. Each logger checks the level of message to its level 
	 * and print accordingly otherwise does not print and pass the message to its next logger.
	 * 1.Create AbstractLogger abstract class.
	 * 2.Create concrete classes likeConsoleLogger, ErrorLogger and FileLogger extending the logger.
	 * 3.Create ChainPatternDemo class to run the class.
	 */
	
}
