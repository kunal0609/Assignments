package CommandPattern;

public class a {

	/*
	 * 1.Create a command interface (Order).
	 * 2.Create a request class (Stock).
	 * 3.Create concrete classes like BuyStock and SellStock implementing the Order interface which 
	 * will do actual command processing.
	 * 4.Create command invoker class (Broker) which acts as an invoker object. It can take and place 
	 * orders.
	 * 5.Create the CommandPatternDemo class which will use the Broker class to take and execute commands.
	 */
	
}
