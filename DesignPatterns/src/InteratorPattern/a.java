package InteratorPattern;

public class a {

	/*
	 * 1.Create interfaces like Iterator and Container.
	 * 2.Create concrete class (NameRepository) implementing the Container interface. This class has inner 
	 * class (NameIterator) implementing the Iterator interface.
	 * 3.Create IteratorPatternDemo class and use the NameRepository to get iterator and print names.
	 */
	
}
