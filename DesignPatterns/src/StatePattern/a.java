package StatePattern;

public class a {

	/*
	 * 1.Create an interface (State).
	 * 2.Create concrete classes like StartState and StopState implementing the State interface.
	 * 3.Create Context Class which carries a State.
	 * 4.Create StatePatternDemo class which uses the Context to see change in behaviour when State changes.
	 */
	
}
