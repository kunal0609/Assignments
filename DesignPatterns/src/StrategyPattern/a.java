package StrategyPattern;

public class a {

	/*
	 * 1.Create Strategy interface defining an action.
	 * 2.Create concrete classes like OperationAdd, OperationSubstract and OperationMultiply implementing 
	 * the Strategy interface.
	 * 3.Create Context Class which uses a Strategy.
	 * 4.Create StrategyPatternDemo class which uses the Context to see change in behaviour when it 
	 * changes its Strategy.
	 */
	
}
