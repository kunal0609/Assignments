package TemplatePattern;

public class a {

	/*
	 * 1.Create a Game abstract class defining operations with a template method set to be final so 
	 * that it cannot be overridden.
	 * 2.Create Cricket and Football concrete classes that are extending Game abstract class and 
	 * override its methods.
	 * 3.Create TemplatePatternDemo class which uses Game to demonstrate use of template pattern.
	 */
	
}
